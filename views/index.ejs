<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css"/>
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f8f8f8;
        }

        .container {
            margin: 0 auto;
            max-width: 860px;
        }

        input {
            width: 70%;
            padding: 10px;
            margin-right: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        button {
            padding: 10px;
            background-color: #4caf50;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        .info-container {
            display: flex;
            gap: 15px;
        }

        #weather-info, #population, #fact {
            width: 30%;
            margin-top: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;

            max-width: 600px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        #map {
            margin-top: 20px;
            height: 300px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
    </style>
</head>
<body>

<div class="container">
    <input id="city" type="text">
    <button onclick="search()">Get info</button>

    <div class="info-container">
        <div id="weather-info">
            <% if (weatherData) { %>
            <p>Temperature: <%= weatherData.main.temp %> F</p>
            <p>Description: <%= weatherData.weather[0].description %></p>
            <!-- Add the rest of the dynamic content here -->
            <% } else { %>
            <p>Failed to fetch weather data.</p>
            <% } %>
        </div>

        <div id="population">
            <% if (populationData) { %>
            <p><strong>Population:</strong> <%= populationData.population %> people</p>
            <% } %>
        </div>

        <div id="fact">
            <% if (factData) { %>
            <p><strong>Fact of the day:</strong> <%= factData.fact %></p>
            <% } %>
        </div>
    </div>

    <div id="map" style="height: 300px;"></div>
</div>

<script src="../public/main.js"></script>
</body>
</html>
